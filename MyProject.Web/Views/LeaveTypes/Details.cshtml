@model LeaveTypeReadOnlyVM

@{
    ViewData["Title"] = $"ID#{Model.Id} - Details about Leave Type";
}

<h1>Details about Leave Type for @Model.Name (id#@Model.Id)</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NumberOfDays)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NumberOfDays)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-danger" asp-route-id="@Model?.Id">Edit</a> 
    <a asp-action="Index" class="btn btn-outline-dark">Back to List</a>
</div>
